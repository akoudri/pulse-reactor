spring:
  application:
    name: pulse-mvc-loom

# TODO (Partie D) : activer les virtual threads (spring.threads.virtual.enabled).
#
# TODO (Partie D) : configurer le port HTTP (8081).
#
# TODO (Partie D) : configurer les mêmes upstreams que le jumeau réactif
#   (pulse.upstream.base-url + endpoints) — contrat identique pour le différentiel.
