package fr.janus.pulse.common;

/**
 * Agrégat de santé des upstreams (résultat du fan-out de la Partie B).
 */
// TODO : définir le record AggregateHealth (état global + détail par upstream).
public record AggregateHealth(/* TODO */) {
}
