package fr.janus.pulse.common;

/**
 * Santé d'un upstream interrogé par le fan-out (Partie B du lab).
 */
// TODO : définir le record UpstreamHealth (nom, état, latence...).
public record UpstreamHealth(/* TODO */) {
}
