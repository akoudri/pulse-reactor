/**
 * Contrats d'API partagés de Pulse (DTOs + pipelines de domaine purs).
 *
 * <p>Package {@code @NullMarked} : sauf annotation {@code @Nullable} explicite,
 * tout type de référence est non-null. Convention null-safety du projet.
 */
@NullMarked
package fr.janus.pulse.common;

import org.jspecify.annotations.NullMarked;
