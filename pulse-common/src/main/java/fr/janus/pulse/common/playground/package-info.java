/**
 * Bac à sable pédagogique : démonstrations de schedulers et de threading Reactor.
 *
 * <p>Package {@code @NullMarked} (convention null-safety du projet). Nommé
 * {@code …pulse.common.playground} et non {@code reactor.playground} pour ne pas
 * entrer en collision avec le namespace {@code reactor.*} de Project Reactor.
 */
@NullMarked
package fr.janus.pulse.common.playground;

import org.jspecify.annotations.NullMarked;
